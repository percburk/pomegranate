# Set default behavior to automatically normalize line endings in git
* text=auto

# Force batch scripts to always use CRLF line endings for Windows
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf

# Force bash scripts to always use LF line endings for Mac/Linux
*.sh text eol=lf
